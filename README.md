Backend Server for the Web Application - Volunteer Match

A Project by Team Nexus, for Zenith'24

Steps to start server locally:
-> git clone "repo"
-> once inside the project folder, run "yarn"
-> then run "yarn dev"
